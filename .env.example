# Clé secrète Stripe — Dashboard > Developers > API keys.
# Utilisez une clé "sk_test_..." tant que vous êtes en développement.
# NE JAMAIS committer le fichier .env une fois renommé (voir .gitignore).
STRIPE_SECRET_KEY=sk_test_votre_cle_secrete

# Secret de signature du webhook — Dashboard > Developers > Webhooks > votre
# endpoint, ou affiché par `stripe listen` en local (voir README.md).
STRIPE_WEBHOOK_SECRET=whsec_votre_secret_webhook

# URL publique de votre frontend une fois déployé (sert à construire les
# success_url / cancel_url / return_url, et pour la config CORS).
APP_URL=https://atchef.fr

# Port d'écoute du serveur backend en local.
PORT=4242

# Identifiants du compte administrateur (back-office SAV). Choisissez un
# email et un mot de passe robustes — jamais ceux d'exemple ci-dessous.
ADMIN_EMAIL=admin@atchef.fr
ADMIN_PASSWORD=changez-ce-mot-de-passe

# Clé secrète servant à signer les sessions admin (JWT). Générez une
# valeur longue et aléatoire, par exemple avec :
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_JWT_SECRET=changez-cette-valeur-aussi
